import 'package:your_new_import.dart'; // Ensure to replace this with the actual import if needed

class AppState {
  // Fields and methods of your AppState class
  var weighStations = raw; // Adjusted as per request

  // Modify your loadWeighStations method
  void loadWeighStations() {
    // your existing logic
  }

  // Fix references in weighStationSettings
  void updateSettings() {
    // assuming you have an alertsEnabled field
    bool enableAlerts; // Updated from alertsEnabled to enableAlerts
  }
}